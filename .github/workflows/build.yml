name: Hidralisk Build Pipeline

on:
  push:
    branches: [main, master]
    tags: ['v*', 'stable/*']
  pull_request:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag para el build'
        required: false
        default: ''

env:
  VERSION: ${{ github.event.inputs.version || github.ref_name || 'ci-$(date +%Y%m%d%H%M)' }}
  GPG_KEY: build@hidralisk.dev
  PYTHONUNBUFFERED: 1

jobs:
  validate:
    name: Quick Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make quick-test

  build:
    name: Build Hidralisk ISO
    needs: validate
    runs-on: [self-hosted, linux, x64]
    steps:
      - uses: actions/checkout@v4

      - name: Instalar dependencias
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            mmdebstrap ostree xorriso gnupg \
            python3-yaml python3-jsonschema \
            libarchive-tools

      - name: Setup GPG key
        run: |
          gpg --batch --quick-gen-key "$GPG_KEY" default default never 2>/dev/null
          gpg --export --armor "$GPG_KEY" > keys/verify.pub

      - name: Quick test pre-build
        run: make quick-test

      - name: Ejecutar pipeline
        run: make build
        env:
          VERSION: ${{ env.VERSION }}

      - name: Smoke test post-build
        run: make smoke-test
        env:
          VERSION: ${{ env.VERSION }}

      - name: Firmar ISO con cosign
        if: ${{ env.COSIGN_KEY != '' }}
        run: |
          cosign sign-blob \
            --key env://COSIGN_KEY \
            --output-signature output/Hidralisk-${{ env.VERSION }}.iso.sig \
            output/Hidralisk-${{ env.VERSION }}.iso
          cosign attest \
            --key env://COSIGN_KEY \
            --predicate output/attestation.intoto.jsonl \
            --type slsa.dev/provenance/v1 \
            output/Hidralisk-${{ env.VERSION }}.iso
        env:
          COSIGN_KEY: ${{ secrets.COSIGN_KEY }}

      - name: Upload ISO
        uses: actions/upload-artifact@v4
        with:
          name: Hidralisk-${{ env.VERSION }}
          path: output/Hidralisk-*.iso*
          retention-days: 30
